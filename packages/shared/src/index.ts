// Shared TS types, zod schemas, and constants used by the api and worker apps.
// Populated as each phase needs cross-app types (feed/article DTOs in Phase 2, etc).
export const APP_NAME = "distill";
